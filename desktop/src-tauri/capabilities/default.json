{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. The frontend has no direct shell/process/filesystem permissions -- every privileged operation goes through our own `bridge_invoke`/`bridge_status` commands, which forward to the allowlisted Python core over the bridge protocol.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
