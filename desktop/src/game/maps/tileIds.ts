/**
 * Local tileset frame ids -- must mirror the layout drawn by
 * `scripts/generate_office_assets.py`. Only the ids referenced directly
 * by scene code (as opposed to ones only ever painted into the tilemap
 * layers themselves) are listed here.
 */
export const TILE_COMPUTER = 32;
